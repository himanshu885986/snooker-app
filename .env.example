# Leave these empty to run in demo mode (data stays in this browser only).
# Fill them in from Supabase → Project Settings → API to use the real database.
# Copy this file to .env and put the values there; .env is never committed.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
